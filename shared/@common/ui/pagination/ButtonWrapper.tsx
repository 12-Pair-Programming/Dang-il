import { ReactNode } from 'react';

const ButtonWrapper = ({ children }: { children: ReactNode }) => {
  return <div className="flex align-center gap-[20px]"></div>;
};

export default ButtonWrapper;
